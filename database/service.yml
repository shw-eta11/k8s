apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: frontend-namespace
spec:
  type: ClusterIP
  selector:
    app: database
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306